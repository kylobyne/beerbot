# 🍺 Beer Bot — Telegram бот с рейтингом выпитого пива

Telegram-бот для групповых чатов, который позволяет пользователям виртуально «пить пиво», получать место в рейтинге и покупать дополнительные попытки через Telegram Stars.

## Возможности

### 🍺 Обычное питьё

Команда:

```
/beer
```

После команды пользователь получает случайное количество виртуального пива.

Особенности:

* работает только в групповых чатах;
* действует кулдаун между обычными попытками;
* выпитые литры добавляются в рейтинг текущего чата;
* время последнего питья сохраняется в базе.

---

### ⭐ Дополнительные попытки

Команда:

```
/buy
```

Открывает меню покупки дополнительных попыток.

Доступные товары настраиваются в `config.py`.

Пример:

```python
BUY_OPTIONS = {
    "5": {
        "attempts": 5,
        "stars": 30
    },

    "10": {
        "attempts": 10,
        "stars": 50
    },

    "15": {
        "attempts": 15,
        "stars": 70
    }
}
```

После успешной оплаты:

* пользователю начисляются дополнительные попытки;
* попытки сохраняются глобально для всех чатов;
* используется база `payd_attemps.sqlite3` - общася для всех чатов.

---

## Логика дополнительных попыток

Если пользователь использует:

```
/beer
```

Бот проверяет:

1. Есть ли обычная доступная попытка.
2. Если действует кулдаун:

   * проверяются купленные попытки;
   * если они есть — одна попытка списывается;
   * литры добавляются в рейтинг текущего чата;
   * время обычного питья (`last_drink`) не изменяется.

Таким образом:

* купленные попытки позволяют пить вне кулдауна;
* рейтинг чата продолжает работать;
* невозможно сбить обычный таймер.

---

## 📊 Статистика

Команда:

```
/stats
```

Показывает рейтинг пользователей в текущем чате.

Возможности:

* сортировка по количеству литров;
* постраничный вывод;
* inline-кнопки навигации.

---

## Команды

| Команда  | Описание                      |
| -------- | ----------------------------- |
| `/start` | Информация о боте             |
| `/beer`  | Выпить виртуальное пиво       |
| `/buy`   | Купить дополнительные попытки |
| `/stats` | Показать рейтинг              |

---

# Структура проекта

```
beer/
│
├── bot.py                 # Запуск бота
├── messages.py            # Все сообщения бота
├── database.py            # Работа с SQLite
│
├── beer.py                # Команда /beer
├── buy.py                 # Покупки Telegram Stars
├── start.py               # Команда /start
├── stats.py               # Рейтинг /stats
│
├── sqlite3/
│   ├── chat_<id>.sqlite3  # Базы отдельных чатов
│   └── payd_attemps.sqlite3 # Общая база платных попыток
│
├── requirements.txt
├── .env
└── README.md
```

---

# Базы данных

## База чата

Каждый чат имеет свою SQLite базу:

```
sqlite3/chat_<chat_id>.sqlite3
```

В ней хранится:

* пользователи;
* количество выпитых литров;
* время последней обычной попытки.

---

## Общая база платных попыток

Файл:

```
sqlite3/payd_attemps.sqlite3
```

Используется для хранения купленных попыток.

Особенности:

* одна база для всех чатов;
* попытки привязаны к Telegram `user_id`;
* пользователь может использовать их в любом чате.

---

# Настройка


```
.env
```

Добавить:

```env
BOT_TOKEN=ваш_токен_бота
```

---

# Установка

Установить зависимости:

```bash
pip install -r requirements.txt
```

---

# Запуск

```bash
python bot.py
```

---

# Зависимости

Основные библиотеки:

```
aiogram
python-dotenv
```

---

# Настройка товаров

Все товары находятся в:

```
options.py
```

Чтобы добавить новый пакет, достаточно добавить новый элемент:

```python
"25": {
    "attempts": 25,
    "stars": 100
}
```

После этого бот автоматически:

* создаст новую кнопку в `/buy`;
* создаст invoice Telegram Stars;
* обработает покупку;
* выдаст нужное количество попыток.

---

# Безопасность покупок

Попытки начисляются только после получения:

```
successful_payment
```

от Telegram.

Без подтверждённой оплаты пользователь не получает дополнительные попытки.

---

# Технологии

* Python 3
* Aiogram 3
* SQLite
* Telegram Bot API
* Telegram Stars
